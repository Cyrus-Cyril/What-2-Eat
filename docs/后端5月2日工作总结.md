# 后端5月2日工作总结

---

## 一、项目分析阶段

### 1.1 已有代码审查

对 3号成员已完成的工作进行了全面审查，确认以下模块全部就绪：

| 文件 | 功能 | 状态 |
|------|------|:--:|
| `restaurant_model.py` | Restaurant dataclass（9字段） | ✅ |
| `amap_client.py` | 高德地图 V5 周边搜索 API 调用 | ✅ |
| `data_cleaner.py` | 原始数据清洗（异常处理、字段解析） | ✅ |
| `main.py` | 对外接口 `get_candidate_restaurants()` | ✅ |
| `test_api_and_cleaner.py` | 8个集成测试用例 | ✅ |
| `字段说明文档.md` | 数据结构与接口说明文档 | ✅ |

### 1.2 计划书分析

阅读并解析了 `今天吃什么项目计划书.docx`（约15,000字），确认：
- 系统采用前后端分离的四层架构（交互层/业务层/数据层/外部服务层）
- 推荐引擎使用"规则评分 + 历史反馈修正"策略
- 定义了5张核心数据库表（用户、查询记录、餐馆、推荐记录、反馈记录）
- 项目当前处于方案设计与立项论证阶段

### 1.3 分布式特性分析

结合老师"分布式主要体现在数据库查询上"的要求，确定了新增职责：
- 分布式缓存层（Redis 缓存餐馆数据）
- 数据库读写分离路由（主库写、从库读）
- 分库路由中间件（按 user_id 哈希）
- 与5号成员协作完成分布式数据库层

### 1.4 任务路线规划

为2号成员制定了 **5阶段、13个任务** 的完整开发路线：

| 阶段 | 内容 | 任务数 |
|:----:|------|:-----:|
| 阶段一 | 地基搭建（DB对齐、框架搭建） | 2 |
| 阶段二 | 核心逻辑（推荐引擎、接口串联） | 3 |
| 阶段三 | 增强模块（反馈机制、大模型接入） | 2 |
| 阶段四 | 分布式特性（缓存、读写分离、分库路由） | 4 |
| 阶段五 | 收尾（联调、答辩准备） | 2 |

---

## 二、基础阶段开发

### 2.1 技术选型

| 维度 | 选型 |
|------|------|
| Web 框架 | FastAPI 0.115.0 |
| ASGI 服务器 | Uvicorn 0.30.0 |
| 数据校验 | Pydantic 2.9.0 |
| HTTP 客户端 | requests 2.31.0 |
| 日志 | Python logging（替换原有 print） |
| 配置管理 | 环境变量 + config.py（不再硬编码 Key） |

### 2.2 完成的任务清单

| # | 任务 | 说明 |
|:-:|------|------|
| 1 | 升级依赖 | 新增 fastapi、uvicorn、pydantic |
| 2 | 创建项目骨架 | `app/api/`、`app/models/`、`app/services/` 三层目录 |
| 3 | 改造 config.py | API Key 支持环境变量覆盖；预留大模型、数据库、Redis 配置项；新增 `setup_logging()` |
| 4 | 定义 Pydantic 模型 | `RecommendRequest/Response`、`FeedbackRequest/Response`、`HistoryResponse`、`HealthResponse` |
| 5 | 实现 API 路由 | `GET /api/health`、`POST /api/recommend`、`POST /api/feedback`、`GET /api/history` |
| 6 | 实现推荐引擎 | 多维度评分（距离30% + 评分30% + 口味20% + 预算20%）+ 规则模板解释 |
| 7 | 创建 FastAPI 入口 | CORS 全开放、lifespan 事件、`/docs` 自动生成 |
| 8 | 旧模块日志升级 | `amap_client.py`、`data_cleaner.py`、旧 `main.py` 的 print 全部改为 logging |
| 9 | 接口测试验证 | 4 个接口全部通过，推荐接口成功返回带评分和理由的餐馆列表 |
| 10 | 编写接口文档 | `API接口文档.md`（供1号前端联调使用） |

### 2.3 验证结果

```
=== health ===
{"status": "ok", "version": "0.1.0"}

=== recommend ===
code=0 total=3
  爷爷不泡茶NOYEYENOTEA | 冷饮店 | score=0.715 | 距离仅185m...
  米釜拌饭 | 餐饮相关 | score=0.688 | 距离仅142m...
  柠檬手打柠檬茶 | 冷饮店 | score=0.350 | 距离仅190m...

=== feedback ===
{"code": 0, "message": "反馈已记录"}

=== history ===
{"code": 0, "data": [], "total": 0}
```

---

## 三、目录结构整理

### 3.1 整理前后对比

**整理前（混乱）：**
```
What-2-Eat/
├── main.py              ← 数据入口（易与 app/main.py 混淆）
├── app/main.py          ← FastAPI 入口
├── amap_client.py       ← 散落在根目录
├── data_cleaner.py      ← 散落在根目录
├── restaurant_model.py  ← 散落在根目录
├── test_api_and_cleaner.py
├── config.py
├── requirements.txt
├── API接口文档.md
├── 字段说明文档.md
└── 今天吃什么项目计划书.docx
```

**整理后（清晰）：**
```
What-2-Eat/
├── config.py                    # 全局配置
├── requirements.txt             # 依赖清单
│
├── app/                         # 所有 Python 代码
│   ├── main.py                  # FastAPI 入口
│   ├── api/
│   │   └── routes.py            # 4个API路由
│   ├── models/
│   │   ├── restaurant.py        # Restaurant dataclass
│   │   └── schemas.py           # Pydantic 请求/响应模型
│   └── services/
│       ├── data_entry.py        # 数据入口 (get_candidate_restaurants)
│       ├── amap_client.py       # 高德 API 调用
│       ├── data_cleaner.py      # 数据清洗
│       └── recommender.py       # 推荐引擎
│
├── tests/                       # 测试代码
│   └── test_api_and_cleaner.py
│
└── docs/                        # 项目文档
    ├── API接口文档.md
    ├── 字段说明文档.md
    └── 今天吃什么项目计划书.docx
```

### 3.2 整理操作

- 3号旧模块移入 `app/models/` 和 `app/services/`（保持原逻辑不变）
- 旧 `main.py` 重命名为 `data_entry.py`（消除与 FastAPI 入口的同名歧义）
- 测试文件移入 `tests/` 目录
- 所有文档移入 `docs/` 目录
- 更新所有 `import` 路径，消除 `sys.path.insert` 硬编码
- 删除根目录 5 个冗余 `.py` 文件

---

## 四、服务运行信息

| 项目 | 信息 |
|------|------|
| 启动命令 | `uvicorn app.main:app --reload` |
| 默认端口 | http://localhost:8000 |
| Swagger 文档 | http://localhost:8000/docs |
| 框架 | FastAPI 0.115.0 |
| Python 版本 | 3.13 |

---

## 五、当前进度总览

| 成员 | 模块 | 进度 |
|:----:|------|:--:|
| 1号 | 前端页面 | ⬜ 待开发 |
| 2号 | 后端主程序（基础阶段） | 🟢 **已完成** |
| 2号 | 推荐引擎（基础评分） | 🟢 **已完成** |
| 2号 | 反馈机制 | 🟡 接口已定义，逻辑待DB就绪 |
| 2号 | 分布式特性 | ⬜ 待开发 |
| 2号 | 大模型接入 | ⬜ 待开发 |
| 3号 | 地图 API + 数据清洗 | 🟢 **已完成** |
| 4号 | 大模型 Prompt 设计 | ⬜ 待开发 |
| 5号 | 数据库设计 | ⬜ 待开发 |

---

## 六、下一步工作

根据此前制定的任务路线，接下来可以推进的方向：

1. **与5号对齐数据库设计** — 最紧迫，阻塞后续反馈机制和分布式
2. **与1号联调** — 已将接口文档放入 `docs/`，前端可开始对接
3. **Redis 缓存层** — 可独立开发，不依赖其他成员
4. **大模型接入** — 需要先调研 API 选型（通义千问/DeepSeek 等）

---

> 📄 本文档位于 `docs/` 目录内，与其他项目文档统一管理。
